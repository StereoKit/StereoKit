name: Verify Compile
on: 
  workflow_dispatch:
  pull_request:
    branches:
    - master
    - develop

jobs:
  native-win32:
    runs-on: windows-latest
    env:
      CPM_SOURCE_CACHE: .deps_cache

    steps:
    - uses: actions/checkout@v4
    - uses: lukka/get-cmake@v3.26.3

    - name: Get Cache
      uses: actions/cache@v3
      with:
        key: DependencyCache
        path: |
          .deps_cache

    - name: Build Native Win32 x64
      run: |
        cmake --preset Win32_x64_Release
        cmake --build --preset Win32_x64_Release

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Install Mesa lavapipe (software Vulkan)
      shell: pwsh
      run: |
        $version = "26.0.7"
        $url = "https://github.com/pal1000/mesa-dist-win/releases/download/$version/mesa3d-$version-release-msvc.7z"
        Invoke-WebRequest -Uri $url -OutFile mesa.7z
        7z x mesa.7z -omesa -y
        $icd = (Get-ChildItem -Path mesa -Recurse -Filter "lvp_icd.x86_64.json" | Select-Object -First 1).FullName
        if (-not $icd) { throw "lavapipe ICD JSON not found in extracted archive" }
        $icdDir = Split-Path -Parent $icd
        Write-Host "Using ICD: $icd"

        # The Vulkan loader on GH Actions runs elevated and ignores VK_DRIVER_FILES /
        # VK_ICD_FILENAMES for security reasons. Register the ICD in the registry
        # instead — the loader reads HKLM\SOFTWARE\Khronos\Vulkan\Drivers regardless
        # of elevation. Value name is the absolute ICD JSON path, value data is a
        # DWORD 0 meaning "enabled".
        $regKey = "HKLM:\SOFTWARE\Khronos\Vulkan\Drivers"
        New-Item -Path $regKey -Force | Out-Null
        New-ItemProperty -Path $regKey -Name $icd -PropertyType DWord -Value 0 -Force | Out-Null

        # Mesa folder on PATH so vulkan_lvp.dll's dependencies resolve.
        "PATH=$icdDir;$env:PATH" >> $env:GITHUB_ENV

    - name: Run Tests
      working-directory: Examples/StereoKitTest
      run: dotnet run -c Release -- -headless -test

  native-macos:
    runs-on: macos-14
    env:
      CPM_SOURCE_CACHE: .deps_cache

    steps:
    - uses: actions/checkout@v4
    - uses: lukka/get-cmake@v3.26.3

    - name: Get Cache
      uses: actions/cache@v3
      with:
        key: DependencyCache
        path: |
          .deps_cache

    - name: Build Native macOS ARM64
      run: |
        cmake --preset MacOS_Arm64_Release
        cmake --build --preset MacOS_Arm64_Release

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Run Tests
      working-directory: Examples/StereoKitTest
      run: dotnet run -c Release -- -headless -test

  native-linux-android-dotnet:
    runs-on: ubuntu-22.04
    env:
      CPM_SOURCE_CACHE: .deps_cache

    steps:
    - uses: actions/checkout@v4
    - uses: lukka/get-cmake@v3.26.3

    - name: Get Cache
      uses: actions/cache@v4
      with:
        key: DependencyCache
        path: |
          .deps_cache
    
    - name: Install Prerequisites
      run: |
        sudo apt-get update
        sudo apt-get install clang llvm lld ninja-build libx11-dev libxfixes-dev libxrandr-dev libxcursor-dev libfontconfig-dev mesa-vulkan-drivers

    - name: Build Native Linux x64
      run: |
        cmake --preset Linux_x64_Release
        cmake --build --preset Linux_x64_Release

    - name: Set up Android NDK
      uses: nttld/setup-ndk@v1.4.2
      id: setup-ndk
      with:
        ndk-version: r25c
        local-cache: true

    - name: Restore Android Symlinks
      run: |
        directory="${{ steps.setup-ndk.outputs.ndk-path }}/toolchains/llvm/prebuilt/linux-x86_64/bin"
        find "$directory" -type l | while read link; do
            current_target=$(readlink "$link")
            new_target="$directory/$(basename "$current_target")"
            ln -sf "$new_target" "$link"
            echo "Changed $(basename "$link") from $current_target to $new_target"
        done

    - name: Install Android SDK Platform
      run: echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platforms;android-32"

    - name: Build Native Android ARM64
      run: |
        cmake --preset Android_Arm64_Release
        cmake --build --preset Android_Arm64_Release
      env:
        NDK: ${{ steps.setup-ndk.outputs.ndk-path }}

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Run Tests
      working-directory: Examples/StereoKitTest
      run: dotnet run -c Release -- -headless -test
